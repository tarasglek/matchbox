# dnsmasq.conf

no-daemon

log-queries

local=/example.com/
domain=example.com
expand-hosts #append local to entries in /etc/hosts
address=/registry.local/127.0.0.1
#will scan /etc/hosts
#leave newline below:
